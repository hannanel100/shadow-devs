import LeadingMentors from "./LeadingMentors";
import { render } from "@testing-library/react";

describe("testing the leading mentors component", () => {
  it("should render the leading mentors component", () => {
    render(<LeadingMentors />);
  });
  
});
